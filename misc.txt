npm install --save socket.io
npm install --save socket.io-client

npm install express express-graphql graphql

http://localhost:8080/graphql
mutation {
  createUser(userInput: {
    email: "hi@hi.com", name:"Will", password: "tester"
  })
  {
    _id
    email
  }
}
mutation {
  createPost(postInput: {
    title: "hihicom", content:"attyigukmjgbf", imageUrl: "tester"
  })
  {
    _id
    title
  }
}
{
  login(
    "email": "hi@hi.com";
    "password": "password"
    ) {
  token
  userId
  }
}
query {
    posts {
        posts {
            _id
            title
            content
        }
    }
    totalPosts
}